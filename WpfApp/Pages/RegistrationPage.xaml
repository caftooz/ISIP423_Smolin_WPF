<Page x:Class="WpfApp.Pages.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Регистрация">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel Width="300">
            <TextBlock Text="РЕГИСТРАЦИЯ" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Почта" Style="{StaticResource HeaderTitle}"/>
            <TextBox x:Name="EmailBox" Margin="0,5,0,15" Height="35" VerticalContentAlignment="Center"/>

            <TextBlock Text="Логин" Style="{StaticResource HeaderTitle}"/>
            <TextBox x:Name="LoginBox" Margin="0,5,0,15" Height="35" VerticalContentAlignment="Center"/>

            <TextBlock Text="Пароль" Style="{StaticResource HeaderTitle}"/>
            <PasswordBox x:Name="PassBox" Margin="0,5,0,05" Height="35" VerticalContentAlignment="Center"/>

            <TextBlock Text="Повторите пароль" Style="{StaticResource HeaderTitle}"/>
            <PasswordBox x:Name="PassRepBox" Margin="0,5,0,25" Height="35" VerticalContentAlignment="Center"/>
            
            <Button x:Name="RegisterButton" Content="Создать аккаунт" Height="40" Click="RegisterButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
