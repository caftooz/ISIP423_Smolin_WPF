<Page x:Class="WpfApp.Pages.ConfirmationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Confirmation">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Width="200"  Grid.Column="1">
                <TextBlock Text="ФИО:"/>
                <TextBox x:Name="NameBox"/>
                <TextBlock Text="E-mail:"/>
                <TextBox x:Name="MailBox"/>
                <TextBlock Text="Адрес доставки:"/>
                <TextBox x:Name="AddressBox"/>
                <ListBox x:Name="CartBoxList" Margin="0,20,0,20" Focusable="False" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Visible" MaxHeight="200">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="0, 0, 0, 10">
                                <TextBlock Text="{Binding Products.Name}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Count, StringFormat={}{0} шт.}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        <StackPanel Width="200" VerticalAlignment="Bottom" Grid.Column="1">
                <TextBlock x:Name="TotalCostBox" Text="Итого:" HorizontalAlignment="Center" FontSize="20"/>
                <Button Content="Заказать" HorizontalAlignment="Center" FontSize="20" Click="MakeOrder"/>
        </StackPanel>


    </Grid>
</Page>
