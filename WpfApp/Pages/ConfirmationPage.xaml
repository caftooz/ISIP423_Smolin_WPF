<Page x:Class="WpfApp.Pages.ConfirmationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Confirmation">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <StackPanel Width="200">
                <TextBlock Text="ФИО:"/>
                <TextBox x:Name="NameBox"/>
                <TextBlock Text="E-mail:"/>
                <TextBox x:Name="MailBox"/>
                <TextBlock Text="Адрес доставки:"/>
                <TextBox x:Name="AddressBox"/>
                <ListBox x:Name="CartBoxList" Margin="0,20,0,20"  Focusable="False" IsHitTestVisible="False">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Products.Name}"/>
                                <TextBlock Text="{Binding Products.Cost}"/>
                                <TextBlock Text="{Binding Count, StringFormat={} - {0} шт.}"/>
                                <TextBlock Text="{Binding TotalCost}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>

            <TextBlock x:Name="TotalCostBox" Text="Итого:" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20, 20, 20, 80" Width="200" Height="30" FontSize="20"/>
            <Button Content="Заказать" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="20" Width="100" Height="30" FontSize="20" Click="MakeOrder"/>

        </Grid>
    </Grid>
</Page>
