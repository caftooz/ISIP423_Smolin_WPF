<Page x:Class="WpfApp.Pages.SeatSelectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Выбор места">

    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="ЭКРАН" HorizontalAlignment="Center" Foreground="Gray" Margin="0,0,0,40" FontSize="20"/>

<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
    
    <StackPanel x:Name="RowsNumbersPanel" Margin="0,0,15,0" VerticalAlignment="Top">
        </StackPanel>

    <ItemsControl x:Name="SeatsItemsControl">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                            <UniformGrid x:Name="SeatsGrid" 
                                         Columns="{Binding Path=Tag, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                         HorizontalAlignment="Left"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <CheckBox Style="{StaticResource SeatStyle}" 
                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                          IsEnabled="{Binding IsAvailable}"
                          Click="Seat_Click"/>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
    
</StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30">
                <Button x:Name="ConfirmBtn"  Margin="10" Padding="10" 
                        Content="Оформить билет"
                        IsEnabled="False"
                        Click="Confirm_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
