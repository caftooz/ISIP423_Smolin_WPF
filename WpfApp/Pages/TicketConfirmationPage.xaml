<Page x:Class="WpfApp.Pages.TicketConfirmationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Подтверждение билета">

    <Grid>
        <Border Background="#1E1E1E" CornerRadius="15" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="40" Width="450">
            <StackPanel>
                <TextBlock Text="ПОДТВЕРЖДЕНИЕ ЗАКАЗА" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <Grid Margin="0,5">
                    <TextBlock Text="Фильм:" Foreground="Gray"/>
                    <TextBlock x:Name="MovieTitle" Text="Аватар 2" HorizontalAlignment="Right" FontWeight="Bold"/>
                </Grid>
                <Grid Margin="0,5">
                    <TextBlock Text="Зал:" Foreground="Gray"/>
                    <TextBlock x:Name="HallInfo" Text="Зал 1 (IMAX)" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Margin="0,5">
                    <TextBlock Text="Дата и время:" Foreground="Gray"/>
                    <TextBlock x:Name="DateTimeInfo" Text="15 фев, 18:00" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Margin="0,5">
                    <TextBlock Text="Место:" Foreground="Gray"/>
                    <TextBlock x:Name="SeatInfo" Text="Ряд 5, Место 12" HorizontalAlignment="Right" Foreground="#E50914"/>
                </Grid>

                <Separator Margin="0,20" Background="#333"/>

                <Grid>
                    <TextBlock Text="К ОПЛАТЕ" FontSize="18" VerticalAlignment="Center"/>
                    <TextBlock x:Name="PriceText" Text="850 руб." FontSize="24" FontWeight="Bold" HorizontalAlignment="Right" Foreground="Gold"/>
                </Grid>

                <Button Content="ПОДТВЕРДИТЬ И ОПЛАТИТЬ" Margin="0,30,0,0" Height="50" 
                    Background="#E50914" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
