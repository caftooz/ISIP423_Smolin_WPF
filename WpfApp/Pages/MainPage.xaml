<Page x:Class="WpfApp.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="1" Text="Дикие ягоды" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Height="30" Width="30" x:Name="CartButton" HorizontalAlignment="Right" Click="CartButton_Click" Margin="2,2,20,2">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
            <Image Source="/Images/Cart.png"/>
        </Button>
        <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" x:Name="ProductListBox" Grid.Row="1">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Focusable" Value="False" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#E6E6FA" BorderBrush="DarkViolet"  BorderThickness="2" CornerRadius="8">
                        <StackPanel Width="200"  Margin="5">
                            <Image Source="{Binding Image}"/>
                            <TextBlock Margin="5" Text="{Binding Name}" HorizontalAlignment="Left" TextWrapping="Wrap"/>
                            <TextBlock Margin="5" Text="{Binding Cost, StringFormat= ‏ {0} руб.}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <Button Content="Добавить в корзину" Click="Button_Click" Height="30" Width="150">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                            </Button>

                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
