<Page x:Class="WpfApp.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="1" Text="Дикие ягоды" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button x:Name="CartButton" Width="50" HorizontalAlignment="Right" Click="CartButton_Click">
            <Image Source="/Images/Cart.png"/>
        </Button>
        <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" x:Name="ProductListBox" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="220">
                        <Image Height="200" Source="{Binding Image}" Stretch="Uniform"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="5" Text="{Binding Name}" HorizontalAlignment="Center"/>
                            <TextBlock Margin="5" Text="{Binding Cost, StringFormat=- {0} руб.}" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <Button Content="Добавить" Click="Button_Click"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
