<Page x:Class="WpfApp.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="1" x:Name="CartListBox" Focusable="False" IsHitTestVisible="False">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Products.Name}"/>
                        <TextBlock Text="{Binding Products.Cost}"/>
                        <TextBlock Text="{Binding Count, StringFormat={} - {0} шт.}"/>
                        <TextBlock Text="{Binding TotalCost}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Column="1" Content="Оформить заказ" Click="Button_Click" Height="50" Width="150" VerticalAlignment="Bottom" Margin="20"/>
        <Button Click="BackButton_Click" Content="Назад" HorizontalAlignment="Left" VerticalAlignment="Top" Width="50" Margin="20"/>
    </Grid>
</Page>
