<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10">
            <TextBlock x:Name="TitleTextBlock" Margin="5" VerticalAlignment="Center"/>
            <Button x:Name="BackButton" Content="Назад" HorizontalAlignment="Right" VerticalAlignment="Center" Click="BackButton_OnClick"/>
            <Button x:Name="ForwardButton" Content="Далее" HorizontalAlignment="Right" VerticalAlignment="Center" Click="ForwardButton_OnClick"/>
        </StackPanel>
        <Frame x:Name="MainFrame" Grid.Row="1" Source="Pages/MainPage.xaml" Navigated="MainFrame_OnNavigated" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
