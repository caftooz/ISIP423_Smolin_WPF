<Page x:Class="WpfApp.Views.AppointmentDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Views"
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AppointmentDetailPage">

    <Page.DataContext>
        <vm:AppointmentDetailViewModel/>
    </Page.DataContext>    
    
    <Grid>
        <Border Background="LightGray"  Width="300" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="{Binding MasterService.Services.Name}"/>
                <TextBlock Text="{Binding MasterService.Users.FirstName}"/>
                <TextBlock Text="{Binding MasterService.Users.LastName}"/>
                <TextBlock Text="{Binding MasterService.Users.Patronymic}"/>
                <TextBlock Text="{Binding DateTime.Date, StringFormat='dd.MM.yyyy'}"/>
                <TextBlock>
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0:00}:{1:00}">
                            <Binding Path="DateTime.Hour" />
                            <Binding Path="DateTime.Minute" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>

                <ComboBox ItemsSource="{Binding PaymentMethods}"
                          SelectedItem="{Binding SelectedPaymentMethod}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Path=Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBox Text="{Binding Comment}"/>

                <Button Content="Записаться"
                        Command="{Binding MakeAnAppointment}"/>

                <Border Visibility="{Binding ErrorMessage,
                    Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="{Binding ErrorMessage}"/>
                </Border>

            </StackPanel>
        </Border>
    </Grid>
</Page>
