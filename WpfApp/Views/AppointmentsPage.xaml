<Page x:Class="WpfApp.Views.AppointmentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      mc:Ignorable="d" Loaded="Page_Loaded" Title="AppointmentsPage">
    <Page.DataContext><vm:AppointmentsViewModel/></Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Назад" Style="{StaticResource MainButton}" HorizontalAlignment="Left"
                Margin="10,8" Command="{Binding BackCommand}"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Calendar Style="{StaticResource MainCalendar}" SelectedDate="{Binding SelectedDT}"/>

            <Grid Grid.Column="1" Margin="10,0,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,8">
                    <TextBlock Style="{StaticResource SubtitleText}" Text="{Binding CurrentService.Name}"/>
                    <TextBlock Style="{StaticResource BodyText}" HorizontalAlignment="Right">
                        <Run Text="{Binding CurrentMaster.FirstName, Mode=OneWay}"/>
                        <Run Text=" "/><Run Text="{Binding CurrentMaster.LastName, Mode=OneWay}"/>
                        <Run Text=" "/><Run Text="{Binding CurrentMaster.Patronymic, Mode=OneWay}"/>
                    </TextBlock>
                </Grid>

                <Border Grid.Row="1" Visibility="{Binding MSAppointments, Converter={StaticResource EmptyCollectionToVisibility}}" Margin="0,0,0,5">
                    <TextBlock Style="{StaticResource CaptionText}" Text="Записей на это число нет!"/>
                </Border>

                <ListBox Grid.Row="2" x:Name="AppListBox" Style="{StaticResource MainListBox}"
                         SelectedItem="{Binding SelectedAppointment}" ItemsSource="{Binding MSAppointments}"
                         Validation.ErrorTemplate="{x:Null}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardBorder}" Width="152" Height="50">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Style="{StaticResource CaptionText}" TextAlignment="Center"
                                               Text="{Binding Path=Date, StringFormat='dd.MM.yyyy'}"/>
                                    <TextBlock Style="{StaticResource SubtitleText}" TextAlignment="Center">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0:00}:{1:00}">
                                                <Binding Path="Hour"/><Binding Path="Minute"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel Width="500"/></ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</Page>
