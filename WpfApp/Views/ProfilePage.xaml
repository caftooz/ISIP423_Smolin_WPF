<Page x:Class="WpfApp.Views.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="900"
      Title="ProfilePage">

    <Page.DataContext>
        <vm:ProfileViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header info -->
        <StackPanel Margin="20,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Назад" Command="{Binding BackCommand}" Margin="0,0,20,0"/>
                <TextBlock FontSize="16" VerticalAlignment="Center">
                    <Run Text="{Binding CurrentUser.LastName}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding CurrentUser.FirstName}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding CurrentUser.Patronymic}"/>
                </TextBlock>
            </StackPanel>
            <TextBlock Text="{Binding CurrentUser.PhoneNumber}" Margin="0,4,0,0" Foreground="Gray"/>
            <Button Content="Выйти из аккаунта" Command="{Binding LogoutCommand}" 
                    HorizontalAlignment="Left" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Tab buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,5">
            <Button Content="Заказы" Command="{Binding ShowOrdersCommand}" Margin="0,0,5,0"/>
            <Button Content="Записи" Command="{Binding ShowAppointmentsCommand}"/>
        </StackPanel>

        <!-- Orders tab -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Orders}" Margin="20,5"
                 Visibility="{Binding ShowOrders, Converter={StaticResource BoolToVisibility}}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 BorderThickness="0" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,0,5" Padding="10" Width="600">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock>
                                    <Run Text="Заказ #"/><Run Text="{Binding Id}"/>
                                    <Run Text=" от "/><Run Text="{Binding OrderDateTime, StringFormat='dd.MM.yyyy'}"/>
                                </TextBlock>
                                <ItemsControl ItemsSource="{Binding OrderProducts}" Margin="0,4,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock>
                                                <Run Text="{Binding Products.Name}"/>
                                                <Run Text=" x"/>
                                                <Run Text="{Binding Quantity}"/>
                                            </TextBlock>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding TotalCost, StringFormat={}{0:0.00} ₽}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding PaymentMethods.Name}" Foreground="Gray"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Appointments tab -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Appointments}" Margin="20,5"
                 Visibility="{Binding ShowAppointments, Converter={StaticResource BoolToVisibility}}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 BorderThickness="0" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="0,0,0,5" Padding="10" Width="600">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Services.Name}" FontWeight="Bold"/>
                                <TextBlock>
                                    <Run Text="{Binding Users1.LastName}"/>
                                    <Run Text=" "/>
                                    <Run Text="{Binding Users1.FirstName}"/>
                                    <Run Text=" "/>
                                    <Run Text="{Binding Users1.Patronymic}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding AppointmentDateTime, StringFormat='dd.MM.yyyy'}" HorizontalAlignment="Right"/>
                                <TextBlock HorizontalAlignment="Right">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0:00}:{1:00}">
                                            <Binding Path="AppointmentDateTime.Hour"/>
                                            <Binding Path="AppointmentDateTime.Minute"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
