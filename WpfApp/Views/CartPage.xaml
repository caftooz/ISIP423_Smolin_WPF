<Page x:Class="WpfApp.Views.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      mc:Ignorable="d" Title="CartPage">
    <Page.DataContext><vm:CartViewModel/></Page.DataContext>
    <Grid Margin="20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="220"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" Margin="0,0,0,8">
            <Button Content="Назад" Style="{StaticResource MainButton}" Command="{Binding BackCommand}"/>
            <TextBlock Style="{StaticResource TitleText}" VerticalAlignment="Center" Margin="15,0,0,0">
                <Run Text="Корзина (" /><Run Text="{Binding ItemCount, Mode=OneWay}"/><Run Text=")"/>
            </TextBlock>
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding Items}" Style="{StaticResource MainListBox}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardBorder}" Width="210">
                        <StackPanel>
                            <Image Source="{Binding Product.Image}" Height="130" Stretch="UniformToFill"/>
                            <TextBlock Style="{StaticResource PriceText}" Text="{Binding FinalPrice, StringFormat={}{0:0.00} ₽}" Margin="0,4,0,0"/>
                            <TextBlock Style="{StaticResource OldPriceText}" Text="{Binding Price, StringFormat={}{0:0.00} ₽}"
                                       Visibility="{Binding Product.ShowDiscount, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBlock Style="{StaticResource BodyText}" Text="{Binding Product.Name}" Margin="0,2,0,4"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="🗑" Style="{StaticResource IconButton}" Command="{Binding RemoveCommand}"/>
                                <Button Content="−" Style="{StaticResource IconButton}" Command="{Binding DecreaseCommand}"/>
                                <TextBlock Style="{StaticResource SubtitleText}" Text="{Binding Quantity}" Margin="10,0" VerticalAlignment="Center"/>
                                <Button Content="+" Style="{StaticResource IconButton}" Command="{Binding IncreaseCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,10,0,0">
            <TextBlock Style="{StaticResource BodyText}" Text="{Binding TotalPrice, StringFormat=Товары: {0:0.00} ₽}"/>
            <TextBlock Style="{StaticResource BodyText}" Text="{Binding DiscountAmount, StringFormat=Скидка: {0:0.00} ₽}"/>
            <TextBlock Style="{StaticResource SubtitleText}" Margin="0,6,0,0"
                       Text="{Binding FinalPrice, StringFormat=Итого: {0:0.00} ₽}"/>
            <Button Content="Заказать" Style="{StaticResource AccentButton}" Margin="0,12,0,0" Command="{Binding OrderCommand}"/>
        </StackPanel>
    </Grid>
</Page>
