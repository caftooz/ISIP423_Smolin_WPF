<Page x:Class="WpfApp.Views.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      mc:Ignorable="d" Loaded="Page_Loaded" Title="ServicesPage">
    <Page.DataContext>
        <vm:ServicesViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource SidebarBorder}" Padding="8">
            <TreeView BorderThickness="0" Background="Transparent">
                <TreeViewItem Header="Тип" ItemsSource="{Binding ServiceTypes}">
                    <TreeViewItem.ItemTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <CheckBox Style="{StaticResource MainCheckBox}" IsChecked="{Binding Path=IsActive}"/>
                                <TextBlock Style="{StaticResource BodyText}" VerticalAlignment="Center" Text="{Binding Path=Item.Name}"/>
                            </DockPanel>
                        </DataTemplate>
                    </TreeViewItem.ItemTemplate>
                </TreeViewItem>
                <TreeViewItem Header="Мастер" ItemsSource="{Binding Masters}">
                    <TreeViewItem.ItemTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <CheckBox Style="{StaticResource MainCheckBox}" IsChecked="{Binding Path=IsActive}"/>
                                <TextBlock Style="{StaticResource BodyText}" VerticalAlignment="Center" Text="{Binding Path=Item.FirstName}"/>
                            </DockPanel>
                        </DataTemplate>
                    </TreeViewItem.ItemTemplate>
                </TreeViewItem>
            </TreeView>
        </Border>

        <Grid Grid.Column="1" Margin="15,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBox Style="{StaticResource MainTextBox}" Text="{Binding FindText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
            <ListBox Grid.Row="1" ItemsSource="{Binding Services}" Style="{StaticResource MainListBox}"
                     x:Name="LListBox" SelectedItem="{Binding SelectedService}"
                     Validation.ErrorTemplate="{x:Null}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}" Width="160" Height="140" Padding="0">
                            <Grid>
                                <Image Source="{Binding Path=Image}" Stretch="UniformToFill"/>
                                <Border Background="White" Opacity="0.85" VerticalAlignment="Bottom" Padding="6,4">
                                    <TextBlock Style="{StaticResource BodyText}" Text="{Binding Path=Name}"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Grid>
    </Grid>
</Page>
