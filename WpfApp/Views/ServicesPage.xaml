<Page x:Class="WpfApp.Views.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Views"
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ServicesPage">

    <Page.DataContext>
        <vm:ServicesViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <TreeView BorderThickness="0" Background="Transparent">
            <TreeViewItem Header="Тип" ItemsSource="{Binding ServiceTypes}">
                <TreeViewItem.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <CheckBox IsChecked="{Binding Path=IsActive}"/>
                            <TextBlock VerticalAlignment="center" Text ="{Binding Path=Item.Name}"/>
                        </DockPanel>
                    </DataTemplate>
                </TreeViewItem.ItemTemplate>
            </TreeViewItem>
            <TreeViewItem Header="Мастер" ItemsSource="{Binding Masters}">
                <TreeViewItem.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <CheckBox IsChecked="{Binding Path=IsActive}"/>
                            <TextBlock VerticalAlignment="Center" Text ="{Binding Path=Item.FirstName}"/>
                        </DockPanel>
                    </DataTemplate>
                </TreeViewItem.ItemTemplate>
            </TreeViewItem>
        </TreeView>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="Поиск"/>
                <TextBox Text="{Binding FindText, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <ListBox Grid.Row="1" ItemsSource="{Binding Services}"
                     SelectedItem="{Binding SelectedService}"
                     BorderThickness="0" Background="Transparent"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Width="156" Height="140"
                                BorderThickness="2" BorderBrush="Black">
                            <Grid>
                                <Image Source="{Binding Path=Image}"
                                       Stretch="UniformToFill"/>

                                <Border Background="White" BorderBrush="Black"
                                        BorderThickness="0,2,0,0"
                                        VerticalAlignment="Bottom">
                                    <TextBlock Margin="5"
                                           Text="{Binding Path=Name}"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Width="500"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Grid>
    </Grid>
</Page>
