<Page x:Class="WpfApp.Views.ManagerAppointmentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      Title="ManagerAppointmentsPage">
    <Page.DataContext>
        <vm:ManagerAppointmentsViewModel/>
    </Page.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,5,0"/>
            <Button Content="Записать клиента" Command="{Binding AddCommand}"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding AppointmentDateTime, StringFormat='dd.MM.yyyy'}" Width="90"/>
                <DataGridTextColumn Header="Время" Width="60">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0:00}:{1:00}">
                            <Binding Path="AppointmentDateTime.Hour"/>
                            <Binding Path="AppointmentDateTime.Minute"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Users.LastName}" Width="100"/>
                <DataGridTextColumn Header="Мастер" Binding="{Binding Users1.LastName}" Width="100"/>
                <DataGridTextColumn Header="Услуга" Binding="{Binding Services.Name}" Width="120"/>
                <DataGridTextColumn Header="Статус" Width="80">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsCompleted" Converter="{StaticResource BoolToStatusConverter}"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Действия" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="завершить" Margin="2"
                                        Command="{Binding DataContext.CompleteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="перенести" Margin="2"
                                        Command="{Binding DataContext.RescheduleCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
