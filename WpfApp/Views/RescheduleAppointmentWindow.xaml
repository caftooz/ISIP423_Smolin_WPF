<Window x:Class="WpfApp.Views.RescheduleAppointmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Перенос записи" Height="220" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="15">
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Margin="0,0,5,0">
                <TextBlock Text="Дата"/>
                <DatePicker x:Name="DatePick"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Время (чч:мм)"/>
                <TextBox x:Name="TimeBox"/>
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Отмена" Width="80" Margin="0,0,5,0" Click="Cancel_Click"/>
            <Button Content="Перенести" Width="80" Click="Save_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
