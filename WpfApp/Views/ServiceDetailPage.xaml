<Page x:Class="WpfApp.Views.ServiceDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:WpfApp.ViewModels"
      mc:Ignorable="d" Loaded="Page_Loaded" Title="ServiceDetailPage">
    <Page.DataContext><vm:ServiceDetailViewModel/></Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Назад" Style="{StaticResource MainButton}" HorizontalAlignment="Left"
                Margin="10,8" Command="{Binding BackCommand}"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Grid VerticalAlignment="Top" Height="200">
                    <Image Source="{Binding CurrentService.ServiceTypes.Image}" Stretch="UniformToFill"/>
                    <Border Style="{StaticResource CardBorder}" Height="150" Width="220"
                            Margin="-200,0,0,0" Opacity="0.9">
                        <StackPanel>
                            <TextBlock Style="{StaticResource TitleText}" Text="{Binding CurrentService.Name}"/>
                            <TextBlock Style="{StaticResource BodyText}" TextWrapping="Wrap" Text="{Binding CurrentService.Description}"/>
                            <TextBlock Style="{StaticResource PriceText}" Text="{Binding CurrentService.Price, StringFormat={}{0:0.00} ₽}" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
                <TextBlock Style="{StaticResource SubtitleText}" Text="Выбор мастера" Margin="20,15,0,8"/>
                <ListBox x:Name="LListBox" Style="{StaticResource MainListBox}" HorizontalAlignment="Center"
                         ItemsSource="{Binding ServiceMasters}" SelectedItem="{Binding SelectedMaster}"
                         Validation.ErrorTemplate="{x:Null}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardBorder}" Width="420">
                                <Grid>
                                    <TextBlock Style="{StaticResource BodyText}">
                                        <Run Text="{Binding Path=FirstName, Mode=OneWay}"/>
                                        <Run Text=" "/><Run Text="{Binding Path=LastName, Mode=OneWay}"/>
                                        <Run Text=" "/><Run Text="{Binding Path=Patronymic, Mode=OneWay}"/>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource BodyText}" HorizontalAlignment="Right" Text="→"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
