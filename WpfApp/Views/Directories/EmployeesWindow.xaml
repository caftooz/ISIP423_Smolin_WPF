<Window x:Class="ServiceDesk.Views.Directories.EmployeesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Справочник: Сотрудники"
        Height="560" Width="680"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource LightBgBrush}">
    <Border Margin="16" Background="White" CornerRadius="8"
            BorderBrush="{StaticResource BorderBrush2}" BorderThickness="1">
        <DockPanel Margin="20">
            <TextBlock DockPanel.Dock="Top" Text="Сотрудники"
                       FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource TextDarkBrush}"
                       Margin="0,0,0,16"/>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="+ Добавить"  Style="{StaticResource AccentButton}"
                        Margin="0,0,8,0" Click="BtnAdd_Click"/>
                <Button Content="✏ Изменить" Style="{StaticResource NeutralButton}"
                        Margin="0,0,8,0" Click="BtnEdit_Click"/>
                <Button Content="🗑 Удалить"  Style="{StaticResource DangerButton}"
                        Click="BtnDelete_Click"/>
            </StackPanel>
            <Button DockPanel.Dock="Bottom" Content="Закрыть"
                    Style="{StaticResource NeutralButton}"
                    HorizontalAlignment="Right" Margin="0,10,0,0"
                    Click="BtnClose_Click"/>
            <DataGrid x:Name="Grid">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"        Binding="{Binding Id}"       Width="50"/>
                    <DataGridTextColumn Header="ФИО"       Binding="{Binding FullName}"  Width="*"/>
                    <DataGridTextColumn Header="Телефон"   Binding="{Binding Phone}"     Width="140"/>
                    <DataGridTextColumn Header="Должность" Binding="{Binding Position}"  Width="160"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Border>
</Window>
